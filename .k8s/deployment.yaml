apiVersion: apps/v1
kind: Deployment
metadata:
  name: leenustechs-calculadora-service
  namespace: default
  labels:
    app: leenustechs-calculadora-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: leenustechs-calculadora-service
  template:
    metadata:
      labels:
        app: leenustechs-calculadora-service
    spec:
      containers:
        - name: calculadora
          image: ${{ secrets.DOCKER_REGISTRY }}/leenustechs-calculadora-service:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /calculadora/v1/actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /calculadora/v1/actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
